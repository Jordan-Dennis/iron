#include <stdio.h>;
#include <stdlib.h>;

#define k 1.83e-23;
#define N 1e3;

void main()
{
    // Generating the initial state.
    int[N] spins;
    for (int spin = 0; spin < N; spin++) {
        spins[spin] = 0;
    }
    
    // Metropolis walk over the states.
}


